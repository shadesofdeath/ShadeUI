namespace ShadeUI.Controls;

/// <summary>Where a control renders its icon relative to its content.</summary>
public enum IconPlacement
{
    /// <summary>The icon is drawn before the content.</summary>
    Left,

    /// <summary>The icon is drawn after the content.</summary>
    Right,
}
